# dependencies & build output
node_modules
dist
.lighthouseci

# env & deploy
.env
.env.*
!.env.example
.vercel

# editor / os / local tooling
.claude
.DS_Store
*.local
*.log

# full-resolution originals of the site's images (kept locally, never published)
design/source-images/
